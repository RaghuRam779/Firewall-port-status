# Used by docker-compose.yml
JWT_SECRET=replace-with-a-long-random-string
# Optional: comma-separated allowlist of targets this deployment may scan, e.g.
# SCAN_TARGET_ALLOWLIST=scanme.nmap.org,10.0.0.5
SCAN_TARGET_ALLOWLIST=
